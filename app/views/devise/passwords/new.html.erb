<%= stylesheet_link_tag 'devise_Sign', media: 'all', 'data-turbolinks-track': 'reload' %>

<div class="background">
  <div class="container">
    <div class="screen">
      <div class="screen-body">
        <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <div class="screen-body-item">
          <div class="app-form">
            <div class="app-form-group">
              <%= f.email_field :email, autofocus: true, placeholder: "EMAIL", class:"app-form-control" %>
            </div>
            <div class="app-form-group buttons">
              <%= f.submit "Envoyez-moi les instructions de réinitialisation du mot de passe",class:"app-form-button"%>
            </div>
          </div>
        </div>
      <% end %>
      </div>
    </div>
  </div>
</div>