<% if @user.artist == false %>
 <%= render 'artlover' %>
<% else %>
<% if user_signed_in? && @user.id == current_user.id || current_user.admin == true %>
	<%= render 'artist' %>
	<% else %>
	<%= render 'artist_' %>
	<%end%>
<% end %>










